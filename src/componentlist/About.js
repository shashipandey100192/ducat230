import React from 'react'
import { Abc } from './Contactus'

function About() {
  const b = 800000;
  const a = `Next, we need to import the reducer function from the counter
 slice and add it to our store. ${b} By defining a field inside theeeeeeeeeeeeeeeeeeeeeeeee`;
  return (
    <div>About {a}
      <Abc/>
    </div>

  )
}

export default About